# .NET Sandbox Workspace

Welcome to your .NET experimentation workspace! This repository contains a fully-featured ASP.NET Core MVC application for trying out various .NET concepts.

## 🚀 Quick Start

```bash
cd SandboxApp
dotnet run
```

Then open your browser to `https://localhost:5001` or `http://localhost:5000`

## 📋 What's Included

This sandbox application includes examples and templates for:

- ✅ **Forms** - Text inputs, validation, dropdowns, checkboxes
- ✅ **Partial Views** - Reusable UI components
- ✅ **View Components** - Components with business logic
- ✅ **AJAX** - Dynamic content loading without page refresh
- ✅ **Bootstrap 5** - Modern, responsive UI
- ✅ **Model Validation** - Client and server-side validation

## 📖 Documentation

See the [SandboxApp README](SandboxApp/README.md) for detailed documentation.

## 🎯 Project Structure

```
.Net_Sandbox/
└── SandboxApp/              # ASP.NET Core MVC Application
    ├── Controllers/         # MVC Controllers
    ├── Models/             # Data models
    ├── Views/              # Razor views
    ├── ViewComponents/     # View components
    └── wwwroot/            # Static files
```

## 💡 Using the Sandbox

Navigate to the **🧪 Sandbox** section in the application menu to access all experimentation features. Each section includes:

- Working examples
- Code you can modify
- Comments explaining key concepts
- Tips for experimentation

## 🛠️ Development Commands

```bash
# Run the application
cd SandboxApp && dotnet run

# Run with hot reload (auto-refresh on changes)
cd SandboxApp && dotnet watch run

# Build the application
cd SandboxApp && dotnet build

# Run tests (when added)
cd SandboxApp && dotnet test
```

## 🎉 Happy Coding!

This is your playground - experiment, break things, learn, and have fun!
